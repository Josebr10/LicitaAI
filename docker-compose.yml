version: '3.8'

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: licitaai
      POSTGRES_PASSWORD: licitaai123
      POSTGRES_DB: licitaai
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
